Place installer/autoinstall files here. Files copied to this dir will be included in the installer environment.

For example, `user-data` and `meta-data` from `iso/autoinstall/` should be copied here by `build-live.sh`.
